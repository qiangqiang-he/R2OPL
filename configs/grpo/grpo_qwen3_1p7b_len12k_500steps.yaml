# @package _global_

# GRPO: Qwen3-1.7B Student, 12K training rollouts.

defaults:
  - /grpo
  - _self_

run_name: grpo_qwen3_1p7b_len12k_500steps
run_name_prefix: grpo_qwen3_1p7b_len12k_500steps
student_model_family: qwen3

data:
  max_prompt_length: 2048
  max_response_length: 20480

actor_rollout_ref:
  model:
    path: /mnt/cephfs/LLM_MODEL_HUB/qwen/Qwen3-1.7B
  actor:
    ppo_max_token_len_per_gpu: 14337
    optim:
      lr: 1.0e-6
  rollout:
    temperature: 1.0
    top_p: 1.0
    top_k: -1
    max_model_len: 22528
    max_num_batched_tokens: 22528
    log_prob_max_token_len_per_gpu: 14337
    val_kwargs:
      temperature: 0.6
      top_p: 0.95
      top_k: -1
      do_sample: true

rlvr_generation:
  train_max_new_tokens: 12288
  val_max_new_tokens: 20480

trainer:
  total_training_steps: 500
  val_before_train: true
  test_freq: 20
  save_freq: 20
  experiment_name: ${run_name}
  default_local_dir: ./outputs/${run_name}
